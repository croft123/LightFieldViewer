you need OpenGL, OpenCV, OpenGL extension(glew) added in your libary.
Remeber to change the data set path in the main function.
The data set cosists of a set of bitmap images, and the description file(e.g. Alice.xml).
the text file _parameters.txt contains the corners of the uv and st place, and the viewer will generate _data file itself.